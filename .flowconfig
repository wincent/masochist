[ignore]

[include]

[libs]
./flow-typed
./src/support/flow-libdefs.js

[lints]
all=warn

[options]
module.ignore_non_literal_requires=true

# Used in src to indicate Flow limitations.
suppress_type=$FlowIssue

# Used internally in graphql package.
suppress_type=$FlowFixMe
