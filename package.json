{
  "name": "masochist-experimental",
  "version": "0.0.1",
  "description": "Website infrastructure for overengineers",
  "main": "index.js",
  "author": "Greg Hurrell <greg@hurrell.net>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "*",
    "@types/node": "*",
    "jest": "*",
    "prettier": "*",
    "prettier-v2-for-jest-inline-snapshots": "npm:prettier@^2",
    "ts-jest": "*",
    "typescript": "*"
  },
  "scripts": {
    "build": "tsc --build",
    "build:clean": "tsc --build --clean",
    "build:dry": "tsc --build --dry",
    "build:force": "tsc --build --force",
    "build:watch": "tsc --build --watch",
    "format": "prettier --loglevel warn --write '**/*.{graphql,js,json,md,ts,yml}'",
    "format:check": "prettier --loglevel warn --check '**/*.{graphql,js,json,md,ts,yml}'",
    "lint": "support/lint.sh",
    "test": "jest"
  }
}
