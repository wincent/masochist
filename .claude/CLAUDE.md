# Verification

Before committing, verify changes by running:

1. `cargo check` — confirm the code compiles
2. `bin/test` — run the test suite
3. `bin/check-format` — check formatting
4. `bin/clippy` — run lint checks
