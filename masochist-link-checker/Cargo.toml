[package]
name = "masochist-link-checker"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }
scraper = "0.22"
strsim = "0.11"
tokio = { version = "1", features = ["full"] }
url = "2"
