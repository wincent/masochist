---
tags: bash
---
= Performing a task for all items in a directory =

<pre>cd the_directory
for ITEM in `ls`
do
  echo $ITEM
done</pre>

= Getting input from the user =

One of many possible examples:

<pre>continue_or_abort()
{
  echo
  read -n 1 -p "Press Y to continue, A to abort: " RESPONSE
  if [ "$RESPONSE" = "Y" -o "$RESPONSE" = "y" ]; then
    echo
    return
  else
    echo "Aborting!"
    exit 1
  fi
}</pre>

Another example:

<pre>read -p "Please enter a tag for the backup archives [$OLD_VERSION]: " TAG
if [ "$TAG" != "" ]; then
  OLD_VERSION="$TAG"
fi</pre>

= See also =

* [[Zapping resource forks from the commandline]]
